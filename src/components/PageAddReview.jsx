import { Header } from "./Header"
import { AddReview } from './AddReview'

export const PageAddReview= () => {
    return (
        <main>
            <Header/>
            <AddReview/>
        </main>
    )
}